require("dotenv").config();
const { dumpKeys } = require("./app/actions.ts"); // Note: TS won't run natively in node without ts-node

console.log("TS node execution required. Moving to alternative...");
